// Aca se puede agregar la lógica para manejar las reservas, cargar médicos, especialidades y obras sociales
document.addEventListener('DOMContentLoaded', () => {
    // Cargar médicos, especialidades y obras sociales desde data.js 
});

// Función para manejar la reserva de turnos
document.getElementById('reservation-form').addEventListener('submit', (e) => {
    e.preventDefault();
    // Lógica para procesar la reserva
});
